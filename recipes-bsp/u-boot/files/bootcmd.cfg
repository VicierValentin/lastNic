CONFIG_BOOTCOMMAND="saveenv;if test ${boot_fit} -eq 1; then run update_to_fit; fi; run findfdt; run init_console; if size mmc 0:2 /usr/bin/authorize;then setenv boot_targets mmc0;echo found;run envboot;run distro_bootcmd;else setenv boot_targets mmc1;echo No valid OS or SD card, booting on emmc;run distro_bootcmd;fi"
